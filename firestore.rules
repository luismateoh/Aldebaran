rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para eventos
    match /events/{eventId} {
      // Permitir lectura a todos los usuarios
      allow read: if true;
      
      // Permitir escritura solo a administradores autenticados
      allow write: if request.auth != null && 
        request.auth.token.email == 'luismateohm@gmail.com';
    }
    
    // Reglas para comentarios
    match /comments/{commentId} {
      // Permitir lectura a todos los usuarios
      allow read: if true;
      
      // Permitir crear comentarios a usuarios autenticados o anónimos con email
      allow create: if request.resource.data.author != null && 
        request.resource.data.content != null &&
        request.resource.data.eventId != null;
      
      // Permitir actualizar solo el autor del comentario o admin
      allow update: if (request.auth != null && 
        request.auth.token.email == resource.data.email) ||
        (request.auth != null && 
        request.auth.token.email == 'luismateohm@gmail.com');
      
      // Permitir eliminar solo al admin
      allow delete: if request.auth != null && 
        request.auth.token.email == 'luismateohm@gmail.com';
    }
    
    // Reglas para usuarios
    match /users/{userId} {
      // Permitir lectura solo al propio usuario o admin
      allow read: if request.auth != null && 
        (request.auth.uid == userId || 
        request.auth.token.email == 'luismateohm@gmail.com');
      
      // Permitir escritura solo al propio usuario o admin
      allow write: if request.auth != null && 
        (request.auth.uid == userId || 
        request.auth.token.email == 'luismateohm@gmail.com');
    }
  }
}